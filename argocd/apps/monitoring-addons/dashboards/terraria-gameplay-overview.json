{
  "annotations": { "list": [] },
  "editable": true,
  "panels": [
    {
      "id": 1,
      "type": "stat",
      "datasource": { "type": "prometheus", "uid": "prometheus" },
      "title": "Players Online",
      "targets": [{ "expr": "max(terraria_players_online) or vector(0)" }],
      "gridPos": { "h": 4, "w": 4, "x": 0, "y": 0 }
    },
    {
      "id": 2,
      "type": "stat",
      "datasource": { "type": "prometheus", "uid": "prometheus" },
      "title": "Players Max",
      "targets": [{ "expr": "max(terraria_players_max) or vector(8)" }],
      "gridPos": { "h": 4, "w": 4, "x": 4, "y": 0 }
    },
    {
      "id": 3,
      "type": "stat",
      "datasource": { "type": "prometheus", "uid": "prometheus" },
      "title": "TCP Reachability",
      "targets": [{ "expr": "max(probe_success{job=\"terraria-tcp-probe\"}) or vector(0)" }],
      "gridPos": { "h": 4, "w": 4, "x": 8, "y": 0 }
    },
    {
      "id": 4,
      "type": "stat",
      "datasource": { "type": "prometheus", "uid": "prometheus" },
      "title": "API Source Up",
      "targets": [{ "expr": "max(terraria_exporter_source_up) or vector(0)" }],
      "gridPos": { "h": 4, "w": 4, "x": 12, "y": 0 }
    },
    {
      "id": 5,
      "type": "stat",
      "datasource": { "type": "prometheus", "uid": "prometheus" },
      "title": "World Parser Up",
      "targets": [{ "expr": "max(terraria_world_parser_up) or vector(0)" }],
      "gridPos": { "h": 4, "w": 4, "x": 16, "y": 0 }
    },
    {
      "id": 6,
      "type": "stat",
      "datasource": { "type": "prometheus", "uid": "prometheus" },
      "title": "Log Tracker Up",
      "targets": [{ "expr": "max(terraria_log_tracker_up) or vector(0)" }],
      "gridPos": { "h": 4, "w": 4, "x": 20, "y": 0 }
    },
    {
      "id": 7,
      "type": "stat",
      "datasource": { "type": "prometheus", "uid": "prometheus" },
      "title": "Parser Unsupported",
      "targets": [{ "expr": "max(terraria_world_parser_unsupported_version) or vector(0)" }],
      "gridPos": { "h": 4, "w": 6, "x": 0, "y": 4 }
    },
    {
      "id": 8,
      "type": "stat",
      "datasource": { "type": "prometheus", "uid": "prometheus" },
      "title": "Conn Attempts (Window)",
      "targets": [{ "expr": "max(terraria_log_connection_attempts_window) or vector(0)" }],
      "gridPos": { "h": 4, "w": 6, "x": 6, "y": 4 }
    },
    {
      "id": 9,
      "type": "stat",
      "datasource": { "type": "prometheus", "uid": "prometheus" },
      "title": "World Saves (Window)",
      "targets": [{ "expr": "max(terraria_log_world_saves_window) or vector(0)" }],
      "gridPos": { "h": 4, "w": 6, "x": 12, "y": 4 }
    },
    {
      "id": 10,
      "type": "stat",
      "datasource": { "type": "prometheus", "uid": "prometheus" },
      "title": "World Snapshot Age (s)",
      "targets": [{ "expr": "max(terraria_world_snapshot_age_seconds) or vector(0)" }],
      "gridPos": { "h": 4, "w": 6, "x": 18, "y": 4 }
    },
    {
      "id": 11,
      "type": "timeseries",
      "datasource": { "type": "prometheus", "uid": "prometheus" },
      "title": "Players Online Over Time",
      "targets": [{ "expr": "max(terraria_players_online) or vector(0)", "legendFormat": "Players" }],
      "gridPos": { "h": 8, "w": 12, "x": 0, "y": 8 }
    },
    {
      "id": 12,
      "type": "timeseries",
      "datasource": { "type": "prometheus", "uid": "prometheus" },
      "title": "Activity From Logs",
      "targets": [
        { "expr": "max(terraria_log_connection_attempts_window) or vector(0)", "legendFormat": "Conn attempts" },
        { "expr": "max(terraria_log_world_saves_window) or vector(0)", "legendFormat": "World saves" }
      ],
      "gridPos": { "h": 8, "w": 12, "x": 12, "y": 8 }
    },
    {
      "id": 13,
      "type": "timeseries",
      "datasource": { "type": "prometheus", "uid": "prometheus" },
      "title": "Source Health",
      "targets": [
        { "expr": "max(terraria_exporter_source_up) or vector(0)", "legendFormat": "API source" },
        { "expr": "max(terraria_world_parser_up) or vector(0)", "legendFormat": "World parser" },
        { "expr": "max(terraria_log_tracker_up) or vector(0)", "legendFormat": "Log tracker" }
      ],
      "gridPos": { "h": 8, "w": 12, "x": 0, "y": 16 }
    },
    {
      "id": 14,
      "type": "timeseries",
      "datasource": { "type": "prometheus", "uid": "prometheus" },
      "title": "World Snapshot Freshness",
      "targets": [
        { "expr": "max(terraria_world_snapshot_age_seconds) or vector(0)", "legendFormat": "Age (s)" },
        { "expr": "max(terraria_world_snapshot_mtime_seconds) or vector(0)", "legendFormat": "mtime (epoch)" }
      ],
      "gridPos": { "h": 8, "w": 12, "x": 12, "y": 16 }
    },
    {
      "id": 15,
      "type": "text",
      "title": "Notes",
      "options": {
        "mode": "markdown",
        "content": "Este dashboard prioriza métricas que funcionam no modo vanilla.\\n\\n`Parser Unsupported = 1` indica que a versão atual do `.wld` não é suportada pelo parser instalado.\\n\\n`API Source Up = 0` é esperado quando o endpoint HTTP do servidor não está ativo."
      },
      "gridPos": { "h": 4, "w": 24, "x": 0, "y": 24 }
    }
  ],
  "schemaVersion": 39,
  "style": "dark",
  "tags": ["terraria", "gameplay", "vanilla", "runtime"],
  "templating": { "list": [] },
  "time": { "from": "now-6h", "to": "now" },
  "title": "Terraria Gameplay Overview",
  "version": 2
}
